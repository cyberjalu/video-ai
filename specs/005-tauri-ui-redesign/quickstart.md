# Quickstart: Testing the UI

To view the redesigned Tauri UI:

1. Start the React dev server:
   ```bash
   npm run dev
   ```
2. Spawn the Tauri window:
   ```bash
   npm run tauri dev
   ```
3. Test window dragging on the title bar area. Ensure UI elements are responsive.
